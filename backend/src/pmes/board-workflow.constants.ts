/** Majority of five board seats required to recommend approval (before Secretary resolution). */
export const BOD_MAJORITY_APPROVALS = 3;

/** Nominal BOD size for UI copy (votes are recorded from BOARD_DIRECTOR staff accounts). */
export const BOD_DIRECTOR_SEATS = 5;
