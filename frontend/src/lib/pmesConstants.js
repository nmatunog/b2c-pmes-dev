/** Mirrors App.jsx RESUMABLE_APP_STATES for shared helpers (no React import). */
export const RESUMABLE_APP_STATES = new Set([
  "consent",
  "registration",
  "seminar",
  "exam",
  "result",
  "certificate",
  "loi_form",
]);
